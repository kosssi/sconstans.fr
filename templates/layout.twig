<!DOCTYPE HTML>
<html>
<head>
    <meta http-equiv="Content-Type" content="text/html; charset=UTF-8" />
    <meta name="author" content="Simon CONSTANS" />

    <title>{% block title '' %} - {{ app.title }}</title>

    <link href="{{ app.request.basepath }}/css/theme_simple_bleu.css" rel="stylesheet" type="text/css" />
    <link rel="icon" type="image/png" href="/favicon.png" />
</head>
<body class="{% for key, page in app.pages %}{% if key == app.request.get('_route') %}{{ page.couleur }}{% endif %}{% endfor %}">

<header>
    <nav>
        <ul id="menu" class="cadre">
            {% for key, page in app.pages %}
            <li class="{% if key == app.request.get('_route') %}actif{% else %}{{ page.couleur }}{% endif %}"><a href="/{{ key }}">{{ page.nom }}</a></li>
            {% endfor %}
        </ul>
    </nav>
</header>

<section>
    {% block content %}{% endblock %}
</section>

<footer>
    <p>Copyright 2012 Simon CONSTANS</p>
</footer>

</body>
</html>